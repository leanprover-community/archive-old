[
    {
        "content": "<p>I realized that we have both <code>module.dual.transpose</code> and <code>linear_map.dual_map</code> doing basically the same thing under different names, except that <code>module.dual.transpose</code> is linear in its argument. Since <code>module.dual.transpose</code> does everything that <code>linear_map.dual_map</code> does (except allowing dot notation), should we settle on one name and get rid of the other?</p>",
        "id": 280949961,
        "sender_full_name": "Antoine Labelle",
        "timestamp": 1651535606
    }
]