[
    {
        "content": "<p>I noticed that there's no instance of <code>alternative</code> for the <code>exceptional</code> from <code>init.meta.exceptional</code>. Should this instance be added? Two reasons I can think of not to do it:</p>\n<ul>\n<li><code>failure</code> would rarely be useful since it doesn't give an informative error message (but then we could still have an instance <code>has_orelse exceptional</code>).</li>\n<li><code>orelse</code> doesn't short-circuit like in Haskell, so can give unintuitive (if you come from Haskell) performance characteristics.</li>\n</ul>",
        "id": 186991132,
        "sender_full_name": "Jannis Limperg",
        "timestamp": 1580394326
    }
]