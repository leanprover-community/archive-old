[
    {
        "content": "<p>I just want to advertise two cool new linters that were just added to <code>Std</code>:</p>\n<ul>\n<li>The \"unreachable tactic\" linter will warn if a tactic is never called, for example if you do something like <code>simp &lt;;&gt; apply foobar</code> you might not notice that <code>foobar</code> doesn't actually exist because <code>simp</code> closed the goal.</li>\n<li>The \"unnecessary <code>&lt;;&gt;</code>\" linter will warn if you use <code>tac1 &lt;;&gt; tac2</code> when <code>tac1</code> only produces one subgoal. This is amazing for lean 3 porting!</li>\n</ul>\n<p>I'm open to upstreaming these to lean core if desired (these use the lean core linter framework).</p>",
        "id": 309432945,
        "sender_full_name": "Mario Carneiro",
        "timestamp": 1668331744
    }
]